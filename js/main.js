//arch text
(function($){
	var $doodleUp = $('#doodle .up');
	
	$doodleUp.arctext({radius: 50, dir: 1});

})(jQuery);

(function($) {
	$.lockfixed("nav",{offset: {top: 10, bottom: 150}});
	
})(jQuery); 

// (function() {
	// spectrum();
	
	// function spectrum(){
		// var hue = 'rgb(' + (Math.floor(Math.random() * 256)) + ',' + (Math.floor(Math.random() * 256)) + ',' + (Math.floor(Math.random() * 256)) + ')';
		// $('.no-cssanimations .up').animate( { color: hue }, 10000);
		// spectrum(); 
	// }
	
// }) (jQuery);	